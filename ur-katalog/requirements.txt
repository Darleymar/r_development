# Laufzeit: keine. Fetcher und App laufen mit der Python-Standardbibliothek
# (ab 3.9), damit auf dem Handy ein "pkg install python" reicht -- kein pip,
# kein Kompilieren.

# Nur fuer die Tests:
pytest>=8.0
